# Phase-3 Backend Configuration

# Database URL
# For SQLite (development): sqlite:///./test.db
# For PostgreSQL (production): postgresql://user:password@host:db_name
DATABASE_URL=sqlite:///./test.db

# JWT Configuration
# Generate a secure secret: openssl rand -hex 32
JWT_SECRET=your-secure-jwt-secret-here-min-32-characters

# OpenAI Configuration (REQUIRED for chat endpoint)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxx

# Anthropic Configuration (Optional - for ToolRouterAgent if needed)
# Get your API key from: https://console.anthropic.com/keys
ANTHROPIC_API_KEY=sk-ant-xxx

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Logging
LOG_LEVEL=INFO

# Environment
ENVIRONMENT=development
